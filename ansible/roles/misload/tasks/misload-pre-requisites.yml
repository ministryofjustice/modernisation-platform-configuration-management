---
# has been added to requirements.txt - this is to ensure it's there if we're not creating a new instance
- name: install pre-requisites to run pywinrm on linux at a windows target
  ansible.builtin.pip:
    name: pywinrm
    state: present
