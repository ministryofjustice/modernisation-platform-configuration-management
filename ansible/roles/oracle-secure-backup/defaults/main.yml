---
osw_temp_dir: /u02/temp/osbws
osw_stage_dir: /u02/stage/osbws
artefacts_s3_bucket_name: mod-platform-image-artefact-bucket20230203091453221500000001
artefacts_s3_bucket_path: hmpps/oracle-secure-backup

osbws_configs:
  - name: osbws
    s3_bucket_name: "{{ db_backup_s3_bucket_name }}"
    run_sbttest: true

osbws_additional_configs: []

# Delius instances have the osbws_wallet in $ORACLE_BASE/wallets
# Prison instances have the osbws_wallet in $ORACLE_HOME/dbs
# This search path will cater for both locations.
cwallet_search_path: /u01/app/oracle

