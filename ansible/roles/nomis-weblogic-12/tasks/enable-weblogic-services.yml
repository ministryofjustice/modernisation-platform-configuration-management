- name: Enable weblogic services
  ansible.builtin.service:
    daemon_reload: true
    name: "{{ item }}"
    enabled: true
    state: started
  loop:
    - weblogic-node-manager
    - weblogic-server
    - WLS_FORMS
    - WLS_REPORTS
    - weblogic-ohs
