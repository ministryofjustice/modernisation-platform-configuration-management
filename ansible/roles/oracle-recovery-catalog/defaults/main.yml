stage: /u02/stage
temp_dir: /u02/stage/temp
rcvcat_db_name: "{{ db_configs[rcvcat].rcvcat_db_name }}"
rc_ssm_passwords:
  - key: "rc_passwords"
    parameter: "/oracle/database/{{ rcvcat_db_name }}/passwords"
    users:
      - rcvcatowner:
      - sys:
      - system:
