---
- name: Perform pre-requesite checks
  command: "sh ./setup.sh -InstallDir {{ sap_bi_platform_installation_directory }}/ -pre_requisite_check ./response.ini ./failedPrereqs.txt"
  become: true
  become_user: bobj
  ignore_errors: true
  args:
    chdir: "{{ sap_bi_platform_extraction_directory }}"

- name: Run Silent Installation
  command: "sh ./setup.sh –InstallDir {{ sap_bi_platform_installation_directory }}/ -r ./response.ini"
  become: true
  become_user: bobj
  args:
    chdir: "{{ sap_bi_platform_extraction_directory }}"
