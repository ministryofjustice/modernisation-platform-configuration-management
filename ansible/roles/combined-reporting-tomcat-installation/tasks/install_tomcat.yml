---
- name: Run installation of BI Platform
  block:
    - name: Perform pre-requesite checks
      ansible.builtin.shell: "./setup.sh -InstallDir {{ sap_bi_platform_installation_directory }}/ -pre_requisite_check ./response.ini ./failedPrereqs.txt"
      ignore_errors: true
      args:
        chdir: "{{ sap_bi_platform_extraction_directory }}"

    - name: Run Silent Installation
      ansible.builtin.shell: "./setup.sh –InstallDir {{ sap_bi_platform_installation_directory }}/ -r ./response.ini"
      args:
        chdir: "{{ sap_bi_platform_extraction_directory }}"
  become: true
  become_user: bobj
