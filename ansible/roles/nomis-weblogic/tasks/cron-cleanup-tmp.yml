---
- name: create cleanup tmp for weblogic
  ansible.builtin.cron:
    name: "cleanup tmp for weblogic"
    user: root
    minute: "0"
    hour: "0"
    job: "/usr/bin/sudo lsof |grep deleted| grep frmweb| grep -v grep | gawk '{print $2}' | xargs kill -9 | logger -p local3.info -t cleanup-tmp"