{{ weblogic_db_name }} =
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = {{ weblogic_db_hostname }})(PORT = {{ weblogic_db_port }}))
    (CONNECT_DATA =
      (SERVER = DEDICATED)
      (SERVICE_NAME = {{ weblogic_db_name }})
    )
  )
