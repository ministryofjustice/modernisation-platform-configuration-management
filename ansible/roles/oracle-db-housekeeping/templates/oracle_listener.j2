{{ listener_log_location }}
{
        monthly
        rotate {{ longp_policy_weeks }}
        compress
        copytruncate
        create 0640 oracle dba
        notifempty
}

{{ listener_xml_location }}
{
        monthly
        rotate {{ longp_policy_weeks }}
        compress
        copytruncate
        create 0640 oracle dba
        notifempty
}
