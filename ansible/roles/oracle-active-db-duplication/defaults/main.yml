stage: /u02/stage
temp_dir: /u02/stage/temp
oracle_install_user: oracle
oracle_install_group: oinstall
db_configs: {}

db_secretsmanager_passwords:
  db:
    secret: "/oracle/database/{{ target_db_name }}/passwords"
    users:
      - sys:
