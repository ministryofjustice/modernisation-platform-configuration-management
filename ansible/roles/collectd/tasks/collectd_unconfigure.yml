---
- name: debug message
  ansible.builtin.debug:
    msg: "Unconfiguring collectd"

# - name: stop collectd
#   ansible.builtin.service:
#     name: collectd
#     state: stopped
#     enabled: yes

# - name: Remove collectd config file
#   ansible.builtin.file:
#     path: "/etc/collectd.conf"
#     state: absent

# - name: Remove collectd config directory
#   ansible.builtin.file:
#     path: "/opt/collectd"
#     state: absent

# - name: Remove collectd types directory
#   ansible.builtin.file:
#     path: "/usr/share/collectd"
#     state: absent