---
# Generate RSA host keysL /etc/ssh/ssh_host_rsa_key, /etc/ssh/ssh_host_ecdsa_key, /etc/ssh/ssh_host_ed25519_key
- name: Generate /etc/ssh/ host keys
  command: ssh-keygen -A
  tags:
    - amibuild
  when: ansible_distribution == 'OracleLinux'
# run stig hardening
# - import_tasks: stig_oracle-linux_8.yml
#   tags:
#     - amibuild
#   when: ansible_distribution == 'OracleLinux'
