---
- name: Run Patch
  shell: "sh {{ sap_bi_platform_extraction_directory }}/setup.sh -r {{ sap_bi_platform_extraction_directory }}/patch_response.ini –InstallDir {{ sap_bi_platform_installation_directory }}/"
  become: yes
  become_user: bobj
